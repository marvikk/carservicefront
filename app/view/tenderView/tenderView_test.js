'use strict';

describe('myApp.tenderView module', function() {

  beforeEach(module('myApp.tenderView'));

  describe('tenderView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var TenderViewCtrl = $controller('TenderViewCtrl');
      expect(TenderViewCtrl).toBeDefined();
    }));

  });
});
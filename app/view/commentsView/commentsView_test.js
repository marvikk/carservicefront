'use strict';

describe('myApp.commentsView module', function() {

  beforeEach(module('myApp.commentsView'));

  describe('commentsView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var CommentsViewCtrl = $controller('CommentsViewCtrl');
      expect(CommentsViewCtrl).toBeDefined();
    }));

  });
});
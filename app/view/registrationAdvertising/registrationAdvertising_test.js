'use strict';

describe('myApp.registrationAdvertising module', function() {

  beforeEach(module('myApp.registrationAdvertising'));

  describe('registrationAdvertising controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var RegistrationAdvertisingCtrl = $controller('RegistrationAdvertisingCtrl');
      expect(RegistrationAdvertisingCtrl).toBeDefined();
    }));

  });
});
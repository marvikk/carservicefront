'use strict';

describe('myApp.carWashView module', function() {

  beforeEach(module('myApp.carWashView'));

  describe('carWashView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var CarWashViewCtrl = $controller('CarWashViewCtrl');
      expect(CarWashViewCtrl).toBeDefined();
    }));

  });
});
'use strict';

describe('myApp.registrationClient module', function() {

  beforeEach(module('myApp.registrationClient'));

  describe('registrationClient controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registrationClientCtrl = $controller('RegistrationClientCtrl');
      expect(registrationClientCtrl).toBeDefined();
    }));

  });
});
'use strict';

describe('myApp.towTruckView module', function() {

  beforeEach(module('myApp.towTruckView'));

  describe('towTruckView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var TowTruckViewCtrl = $controller('TowTruckViewCtrl');
      expect(TowTruckViewCtrl).toBeDefined();
    }));

  });
});
'use strict';

describe('myApp.mountingView module', function() {

  beforeEach(module('myApp.mountingView'));

  describe('mountingView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var MountingViewCtrl = $controller('MountingViewCtrl');
      expect(MountingViewCtrl).toBeDefined();
    }));

  });
});
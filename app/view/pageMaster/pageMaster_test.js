'use strict';

describe('myApp.pageMaster module', function() {

  beforeEach(module('myApp.pageMaster'));

  describe('pageMaster controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PageMasterCtrl = $controller('PageMasterCtrl');
      expect(PageMasterCtrl).toBeDefined();
    }));

  });
});
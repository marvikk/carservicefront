'use strict';

describe('myApp.pageTender module', function() {

  beforeEach(module('myApp.pageTender'));

  describe('pageTender controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PageTenderCtrl = $controller('PageTenderCtrl');
      expect(PageTenderCtrl).toBeDefined();
    }));

  });
});
'use strict';

describe('myApp.officeMaster module', function() {

  beforeEach(module('myApp.officeMaster'));

  describe('officeMaster controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var OfficeMasterCtrl = $controller('OfficeMasterCtrl');
      expect(OfficeMasterCtrl).toBeDefined();
    }));

  });
});
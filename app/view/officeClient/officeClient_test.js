'use strict';

describe('myApp.officeClient module', function() {

  beforeEach(module('myApp.officeClient'));

  describe('officeClient controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var OfficeClientCtrl = $controller('OfficeClientCtrl');
      expect(OfficeClientCtrl).toBeDefined();
    }));

  });
});
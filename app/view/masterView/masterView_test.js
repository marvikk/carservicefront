'use strict';

describe('myApp.masterView module', function() {

  beforeEach(module('myApp.masterView'));

  describe('masterView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var MasterViewCtrl = $controller('MasterViewCtrl');
      expect(MasterViewCtrl).toBeDefined();
    }));

  });
});